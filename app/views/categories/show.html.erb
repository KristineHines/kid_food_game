<p id="notice"><%= notice %></p>

<div class="active" id="category_1">
	<%= render partial: "category", locals: { category: @category = Category.find(1) } %>
</div>
<div class="inactive" id="category_2">
	<%= render partial: "category", locals: { category: @category = Category.find(2) } %>
</div>
<div class="inactive" id="category_3">
	<%= render partial: "category", locals: { category: @category = Category.find(3) } %>
</div>

<%= button_to "Next Category", category_path(2), method: :get, id: "category_button" %>

